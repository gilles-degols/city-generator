from city.City import City

class Bridge(object):

    def __init__(self, un_pos_x, un_pos_y, f_pos_z, f_angle_y, f_angle_z, f_length):
        # Put Bridge at (un_pos_x, un_pos_y, f_pos_z)
        # Scale x f_length
        # Rotate y f_angle_y (degrees)
        # Rotate z f_angle_z (degrees)
        